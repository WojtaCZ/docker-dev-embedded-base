{
    "$schema": "https://json.schemastore.org/claude-code-settings.json",
    "_comment": "Layer 10 — merged over ~/.claude-layers/00-baseline.json by entrypoint.sh. Declares only what embedded-base ADDS to the baseline MCP set.",
    "mcpServers": {
        "fetch": {
            "command": "uvx",
            "args": ["mcp-server-fetch"]
        }
    }
}
